import { defineConfig } from "unocss";

export default defineConfig({
    theme: {
        fontFamily: {
            mc: "Minecraft Ten",
            "mc-mono": "Monocraft",
        },

        colors: {
            cdark: {
                DEFAULT: "#13151a",
            },
        },
    },
});
